
\section{Security Considerations}
\label{sec:security}

Write on
\begin{enumerate}
    \item Write on trust boundaries between mock PT/ DT $\longleftrightarrow$ Bridge $\longleftrightarrow$ Agent   \item Denial of service, replay attacks on Bridge and Agents are an issue (\textit{implementable})
    \item Spoofing of simulation requests and responses are an issue (\textit{implementable})
    \item maintenance of security context in streaming and interactive simulations (\textit{implementable})
    \item put in place PKI and then authenticate requests based on certificates (\textit{implementable but timeline is uncertain})
    \item system is a prime candidate for man-in-the-middle attack. What are the preventive measures?
    \item What is the performance overhead of security enhancements? (\textit{implementable})
\end{enumerate}

Discuss all of these considerations from the CIA (confidentiality - Integrity - Availability) definitions.