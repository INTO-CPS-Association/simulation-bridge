[pytest]
minversion = 8.3
#pythonpath = src src/data src/methods  tests
testpaths =
    tests/unit
addopts =  --show-capture=no --cov=simul8_agent/src --cov-report=term-missing --cov-report=html --ignore=simul8_agent/docs
python_files = test_*.py

log_cli=false
log_level=DEBUG
log_format = %(asctime)s %(levelname)s %(message)s  
log_date_format = %Y-%m-%d %H:%M:%S

#timeout slow tests
timeout=5
